# Salida del spring m�s copada
logging.level.org.springframework.web=TRACE

# Origen de los datos (nombre db)
#spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.url=jdbc:postgresql://localhost:5432/desarrollo_web

# Driver
#spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.driver-class-name=org.postgresql.Driver

# Username y contrase�a
#spring.datasource.username=sa
#spring.datasource.password=
spring.datasource.username=postgres
spring.datasource.password=1230

# Plataforma
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Consola del h2 (es como un pgadmin)
#spring.h2.console.enabled=true

# Autocrear tablas
spring.jpa.hibernate.ddl-auto=update

# Para que ande el archivo data y schema
spring.datasource.initialization-mode=always

# Usuario y contrase�a
spring.security.user.name = root
spring.security.user.password = root

# Que muestre bien la fecha
spring.jackson.date-format="dd/MM/yyyy' 'HH:mm:ss.SSSXXX
spring.jackson.time-zone = America/Argentina/Cordoba